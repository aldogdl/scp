import 'package:flutter/material.dart';

class LstPiezasSimyls extends StatelessWidget {
  
  const LstPiezasSimyls({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}