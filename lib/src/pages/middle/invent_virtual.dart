import 'package:flutter/material.dart';

class InventVirtual extends StatelessWidget {
  const InventVirtual({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}