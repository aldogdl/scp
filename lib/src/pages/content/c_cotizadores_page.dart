import 'package:flutter/material.dart';

class CCotizadoresPage extends StatelessWidget {

  const CCotizadoresPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return const SizedBox(
      child: Text('cotizadores'),
    );
  }
}