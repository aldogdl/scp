import 'package:flutter/material.dart';

class LstContactos extends StatelessWidget {

  const LstContactos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    
    return Container();
  }
}